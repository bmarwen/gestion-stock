{% extends 'base.html.twig' %}

{% block title %}Modifier valeur{% endblock %}

{% block body %}
    <h1>Modifier valeur</h1>
    <br><br>
        <div class="row">
            <div class="col-md-8">
    {{ include('application/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-5 container">
            <a href="{{ path('admin.application_index') }}" class="btn btn-success mr-3">Retourner</a>
            {{ include('application/_delete_form.html.twig') }}
        </div>
{% endblock %}
