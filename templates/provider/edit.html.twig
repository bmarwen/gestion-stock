{% extends 'base.html.twig' %}

{% block title %}Modifier Fournisseur{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-6">
    {{ include('provider/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
</div>
<div class="row mt-5 container">
    <a href="{{ path('admin.provider_index') }}" class="btn btn-success mr-3">Retourner</a>
    {{ include('provider/_delete_form.html.twig') }}
</div>
{% endblock %}
