<form method="post" action="{{ path('command_on_line_delete', {'id': command_on_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ command_on_line.id) }}">
    <button class="btn">Delete</button>
</form>
