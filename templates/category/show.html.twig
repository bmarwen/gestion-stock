{% extends 'base.html.twig' %}

{% block title %}Catégorie {{ category.name }}{% endblock %}
{% block url %}{{ path('admin.category_show', {'id' : category.id }) }}{% endblock %}

{% block body %}
<div class="row flex mt-4 mb-4">
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <a href="{{ path('admin.category_index') }}" class="btn btn-success mr-3">Retourner</a>

    <a href="{{ path('admin.category_edit', {'id': category.id}) }}" class="btn btn-primary mr-3">Modifier</a>
    <br><br>
    {{ include('category/_delete_form.html.twig') }}
{% endblock %}